name: copia
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  moor_flutter: ^3.0.0
  provider: ^4.0.5+1
  majesty: ^0.0.1

  cupertino_icons: ^0.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter

  moor_generator:
  build_runner:

flutter:
  uses-material-design: true
